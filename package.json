{
  "name": "@taroxin/dev-settings-js",
  "version": "0.1.3",
  "description": "Get or configure DevSettings.",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "release": "nr build && bumpp package.json packages/*/package.json --commit --push --no-tag && pnpm -r publish --access public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bumpp": "^8.2.1",
    "esno": "^0.16.3",
    "jest": "^29.3.1",
    "typescript": "^4.9.4"
  }
}
